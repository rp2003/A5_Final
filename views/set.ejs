<!DOCTYPE html>
<html lang="en">
  <link rel="stylesheet" href="/css/main.css" />
  <html data-theme="light"></html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lego Collection</title>
  </head>
  <body>
    <%- include('partials/navbar', {page: '' }) %>
    <div class="container mx-auto">
      <div class="hero bg-base-200">
        <div class="hero-content text-center">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold"><%= set.name %></h1>
            <p class="py-6">
              You can find all the information you need about <%= set.name %>
              here
            </p>
          </div>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <img
            class="w-128 h-64 rounded-lg"
            src="<%= set.img_url %>"
            alt="<%= set.name %>"
          />
        </div>
        <div class="prose">
          <p>
            <span class="font-bold">Set Name: </span>
            <%= set.name %> <br />
            <span class="font-bold">Year Released: </span>
            <%= set.year %> <br />
            <span class="font-bold">Theme: </span>
            <%= set.Theme.name %> <br />
            <span class="font-bold">Number of Parts: </span>
            <%= set.num_parts %> <br /><br />

            <span id="quote"
              >"<%= quote.content %>" <br />
              -<%= quote.author %></span
            >
            <br /><br />
            <a class="btn btn-primary btn-sm" href="/lego/editSet/<%= set.set_num %>">Edit</a>
            <a class="btn btn-primary btn-sm" href="#" onclick="history.back(); return false;">RETURN</a>
          </p>
        </div>
      </div>
    </div>
  </body>
</html>